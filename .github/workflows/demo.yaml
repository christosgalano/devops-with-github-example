name: demo

on:
  workflow_dispatch:
    
permissions:
  contents: read

jobs:
  build-and-test:
    name: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Hello world
        run: echo "Hello world!"
      
      - name: Build
        run: echo "Building our application"

      - name: Test
        run: echo "Testing our application"
  
  deploy:
    name: deploy
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy
        run: echo "Deploying our application"